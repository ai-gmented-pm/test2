<!-- Généré par Amorce (brique : system_design). Modifiez-le depuis le tableau de bord de l'équipe : une édition manuelle ici sera écrasée au prochain push. -->

# System design

_Proposition dérivée du profil : tout ce qui suit se valide ou s'amende en équipe — rien n'est tranché à votre place._

> Aucune entité renseignée dans le profil Amorce : le gabarit ci-dessous
> est rempli avec une entité fictive `exemple`. Remplacez-la par vos
> vraies entités (et renseignez-les dans Amorce pour régénérer).

## Schéma de données (proposition à valider)

### `exemple`
- `id` + `created_at` / `updated_at` — générés par le framework.
- _Attributs à compléter par l'équipe :_ `...`
- Pour chaque attribut, notez le type (texte, entier, date, référence...) et s'il est obligatoire.

### Associations probables — à trancher en équipe
- [ ] Une seule entité pour l'instant : y en a-t-il d'autres qui manquent au profil ?

## Routes / écrans principaux
Le trio minimal par entité — rayez ce qui ne sert pas la démo :
- `exemple` : liste (index), détail (show), création (create)

## Wireframes
Collez les liens (Figma, Excalidraw, photo de paperboard) :
- [ ] Écran d'accueil : <lien>
- [ ] Écran exemple : <lien>

## Décisions techniques ouvertes
- [ ] Hébergement : suggestion du preset — Render (web service + PostgreSQL managé) — confirmé ?
- [ ] Authentification : oui / non ? Si oui, quel mécanisme ?
- [ ] API externe : le projet en consomme-t-il une ? Laquelle ? Clé à obtenir ?
- [ ] Mobile : app native, PWA, ou responsive seulement ?

Chaque décision prise mérite une ligne datée ici (ou un ADR si le sujet est lourd).
